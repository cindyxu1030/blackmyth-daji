---
title: 视频脚本 — Fable 5 做黑神话妲己，强在哪
project: BlackMyth
type: video-script
status: draft
created: 2026-06-10
tags: [blackmyth, fable5, 口播脚本, 小蓝相关]
links:
  based-on: [README]
---

# 视频脚本 — 「我让 AI 写了个黑神话，它顺手把我直播翻车的后路都留好了」

> 9:16 口播 + 游戏录屏 B-roll（Proof 全是真录屏，无需 HyperFrame 概念动画）。
> 预计时长 ~90s。所有数字和事实都来自项目实际产物，没有一条是没验证过的。

| # | 口播 | 画面 |
|---|---|---|
| 1 · Hook | 一天，4000 行代码，一个能打的 3D Boss 战——但真正吓到我的不是这个。 | 游戏高光快剪：杨戬激光扫射、妲己翻滚、九尾分身围攻（`?demo=1` 录屏） |
| 2 · 是什么 | 这是《黑神话：妲己》，浏览器里直接跑。三阶段 Boss：近战连斩、天眼激光、狂暴加哮天犬。我跟 Fable 一次对话，从零写完，12 个模块。 | 三阶段各 1 秒切镜（`1`/`2` 跳阶段录）；切 VSCode 文件树扫过 12 个 js 文件 |
| 3 · 转折 | 但说实话，"能写很多代码"现在每个模型都行。差距在下面这三件——我没要求，它自己决定做的事。 | 黑屏白字：「没要求，它自己做的」 |
| 4 · 强点① 防真实世界 | 第一，它防的是真实世界。它想到我直播时中文输入法会吃按键，所以监听的是物理键位；想到现场可能报错，所以任何崩溃只冻结 Boss AI，画面不白屏；还给我留了救场键——按 0，直接无敌。它替我预演了一整场直播会怎么翻车。 | 录屏：中文输入法状态下照样能打；按 `0` 出现「天命加身」；代码特写 `e.code` 那行 |
| 5 · 强点② 给自己出考卷 | 第二，它给自己出考卷。游戏写完，顺手写了个自测模式：网址后面加 `?selftest=1`，自动打完三阶段、放完所有技能、赢一次、输一次，结果吐成 JSON。别的模型写完说"应该没问题"，它写完是"我测过了，证据在这"。 | 录屏：selftest 自动跑全程加速播放，最后定格左下角 `pass / 0 errors` JSON |
| 6 · 强点③ 手搓 shader | 第三，最离谱的。我把引擎锁死在一个三年多前的旧版本，现成的特效插件全用不了。它没找借口，直接用底层 API 手搓了一整条 HDR 后期管线——bloom、胶片颗粒、调色、暗角，shader 一行行自己写。还加了个帧率看门狗：掉到 40 帧以下，自动关特效保流畅。 | A/B 对比：`?nopost=1` 素颜画面 ↔ 开后期的电影感画面，来回切两次；`post.js` shader 代码快速滚动 |
| 7 · 结尾 | 所以现在模型的差距，早就不是"能不能写出来"，是你没开口的地方，它替你做了多少判断。这场 Boss 战的完整工程已经放出来了——想看我怎么用它，关注小蓝。 | 低机位过肩镜头慢推杨戬，月光柱场景收尾 |

## 事实依据（防吹牛对照表）

- 4000+ 行：js/ 实测 4349 行（boss.js 1057 / player.js 846 / main.js 556 / world.js 554…）+ index.html
- 输入法防御：`e.code` 物理键位 + 0.2s 输入缓冲（见 `js/main.js`）
- 异常不白屏 / `0` 无敌 / `1` `2` 跳阶段：README「演示保险」节
- `?selftest=1` 全流程回归输出 JSON：README + 已实际跑通（v2 回归全绿）
- 旧版引擎：Three.js r147（2022.11 发布）UMD，`post.js` 头部注释明确「禁止 EffectComposer / examples 插件，全部 r147 核心 API 手搓」
- FPS<40 自动关后期：`CONFIG.POST` + FPS 看门狗

## 未验证、脚本里刻意没说的

- 没做过真实的跨模型 A/B（没让 GPT/Gemini 写同一个游戏对比），所以全片不点名其他模型、不报对比数据，只说"我没要求、它自己做的"这个判断力框架。要硬对比得先真跑一轮。
